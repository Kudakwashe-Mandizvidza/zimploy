import { createTheme } from "@mui/system";

export const Theme = createTheme({
  palette: {
    main: "#1760a5",
    light: "skyblue",
  },
  secondary: {
    main: "#15c630",
  },
  otherColor: {
    main: "#999",
  },
});
